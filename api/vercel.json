{
  "version": 2,
  "builds": [
    {
      "src": "index.ts",
      "use": "@vercel/node"
    }
  ],
      "rewrites": [
        { "source": "/(.*)", "destination": "/" }
      ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/index.ts" },
    { "src": "/(.*)", "dest": "/index.ts" }
  ]
}
